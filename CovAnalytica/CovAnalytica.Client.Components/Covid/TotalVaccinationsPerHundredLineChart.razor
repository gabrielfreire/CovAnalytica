@namespace CovAnalytica.Client.Components

<MudGrid>
	<MudItem xs="12">
		<div style="@($"display: {(addedCountries.Count > 0 ? "block" : "none")}")">
			<ApexLineChart @ref="chart" Title="COVID-19 Vaccinations per hundred" YAxisLabel="Vaccinations Per Hundred"></ApexLineChart>
		</div>
		<MudText Typo="Typo.body1" Style="@($"display: {(addedCountries.Count > 0 ? "none" : "block")}")">Add countries to build a chart...</MudText>
	</MudItem>
</MudGrid>

			